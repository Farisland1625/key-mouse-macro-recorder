# Security

KeyMouse Marco Weaver 会捕获并回放当前桌面的键盘和鼠标输入。宏 JSON 可能包含按键、坐标和操作时序，请把它们视为敏感数据：不要录制密码、验证码、令牌、私钥或个人信息，也不要把个人宏文件上传到 Issue 或 Pull Request。

程序默认只在本机运行，不提供联网服务，也不会主动上传宏内容。回放前请确认目标窗口、运行权限和输入状态；不要在不信任的环境中运行来源不明的宏文件或修改后的 exe。

如果发现可能影响用户安全或隐私的漏洞，请不要公开提交复现细节。优先通过 GitHub Security Advisories 私下报告；如果仓库尚未启用该功能，请在 Issue 中只留下不敏感的联系信息，等待维护者回复。
